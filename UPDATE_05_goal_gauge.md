# Оновлення 5 — Кругова шкала цілі на "Огляді"

## Що нового

На "Огляді" картки **"Рентабельність"** і **"Мало на складі"** замінені
на одне вікно розміром у ці дві колонки — **"Ціль на сьогодні"**.

- Кругова шкала заповнюється кольорами в тематику мийки: від аква-блакитного
  (вода) через піняво-зелений (піна) — а якщо ціль перевиконана, кільце
  стає бурштиновим і показує "Ціль перевиконано! 🎉".
- Натискаєш "Задати ціль" → вводиш, наприклад, **5** (авто на день).
- Прогрес рахується автоматично — скільки авто вже додано сьогодні у
  вкладці "Робочий день".
- Опівночі лічильник сам почне рахувати заново, бо орієнтується на
  сьогоднішню дату.

Рентабельність (%) нікуди не зникла — тепер вона показується як
маленька підпис-підказка під карткою "Виплачено працівникам".
"Мало на складі" — залишилось окремим попередженням-банером внизу
сторінки (як і раніше), просто прибрали дублюючу картку зверху. Заразом
додав такий самий банер і для найближчих запланованих записів.

## Крок 1. Онови базу даних у Supabase

Виконай у **SQL Editor**:
```
supabase/migration_05_store_update_policy.sql
```

Це всього один рядок — дозволяє власнику зберігати ціль (без цього
кнопка "Зберегти ціль" видасть помилку доступу).

⚠️ Якщо ще не виконував `migration_04_price_and_materials.sql` з
попереднього разу — виконай і його теж, там додано саме поле
`daily_car_goal`, без якого ця функція не запрацює.

## Крок 2. Онови файли проєкту

Заміни файл (або всю папку, зберігши `.env` і `.git`):
```
src/components/tabs/Overview.jsx    — кругова шкала цілі
supabase/migration_05_store_update_policy.sql — НОВИЙ
```

## Крок 3. Перевір локально

```bash
npm run dev
```

1. Зайди в "Огляд" → в новому вікні натисни "Задати ціль" → введи 5.
2. Перейди в "Робочий день" → додай пару авто на сьогодні.
3. Повернись в "Огляд" → шкала має заповнитись відповідно до кількості
   доданих сьогодні авто.

## Крок 4. Заливай зміни

```bash
git add .
git commit -m "Кругова шкала щоденної цілі на Огляді"
git push
```
